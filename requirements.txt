# Runtime dependencies.
#
# pyproject.toml is authoritative; this file mirrors it for
# `pip install -r requirements.txt` workflows. For the full development
# toolchain (tests, lint, web demo, notebook) use:
#
#     pip install -r requirements-dev.txt
#
# or install the extras directly:
#
#     pip install -e ".[dev]"       # pytest, pytest-cov, ruff
#     pip install -e ".[serve]"     # fastapi, uvicorn
#     pip install -e ".[notebook]"  # jupyter

numpy>=1.24
pandas>=2.0
scipy>=1.10          # chi-squared survival function for the G-test
scikit-learn>=1.3    # logistic-regression and gradient-boosting baselines
matplotlib>=3.7
seaborn>=0.12
